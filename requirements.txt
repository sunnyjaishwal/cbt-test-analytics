# dbt with the Postgres adapter.
# NOTE: dbt-core currently supports Python 3.9–3.12. The venv in ./env is
# Python 3.14, which dbt does not yet support — create the venv with a
# supported interpreter (see README) before installing.
dbt-core>=1.8,<2.0
dbt-postgres>=1.8,<2.0
