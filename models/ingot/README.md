# ingot (gold)

Business-facing marts. Reads from `alloy` models via `ref()`. Aggregates,
metrics, and dimensional models consumed by BI tools and stakeholders.

Materialized as **tables**. Schema: `ingot`.
